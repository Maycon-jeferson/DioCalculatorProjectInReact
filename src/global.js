import { createGlobalStyle } from 'styled-components'
// importa a função createGlobalStyle do pacote styled-components. 

export default createGlobalStyle
//Esta linha define uma função de estilo global usando a sintaxe de template literals (template strings) do JavaScript.
//O export default` permite que esse estilo global seja importado e utilizado em outros arquivos.
`
    *, boby{
        margin: 0;
        padding: 0;
    }
`
//Estas são as regras CSS que definem que tanto as margens quanto o preenchimento de todos os elementos e do corpo da página devem ser zero.